Drop your 7 PNG files into this folder with these EXACT names
(case-sensitive, no spaces):

  profile.png        → professional full-body photo, background removed
  project-sld.png     → SLD AI Explainer screenshot
  project-robot.png   → humanoid robot photo
  project-led.png     → LED cube photo
  project-home.png    → home automation photo
  project-stick.png   → smart blind stick photo
  project-fire.png    → firefighting robot photo

That's it — the site already points at these filenames, so once they're
in this folder (and pushed to GitHub) the images will appear automatically.

Tip: keep each file under ~1–2 MB so pages load fast. If a photo is much
larger, resize it first (e.g. with squoosh.app) before uploading.
